<div class="container">
  <div class="show-container">
    <div class="show-description">
      <div class="booking-description text-center">
        <h4><strongclass="booked-place"><%= @booking.place.name %> </strong>
          <%= @booking.place.continent %></h4>
          <h4 class="text-center">Here you can change your Booking</h4>
          <p>Current booking is</p>
          <p>Check-In :<%= @booking.check_in %></p>
          <p>Check-Out :<%= @booking.check_out %></p>
          <h5 class="text-center"><em>Change the Dates</em></h5>
        </div>
          <div class="form">
            <%= render 'shared/form' %>
          </div>
        </div>
        <div class="show-image" style="background-image: url(<%= @booking.place.photo%> ) "></div>
      </div>
    </div>
