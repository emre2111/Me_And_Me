<%= link_to place_path(place) do  %>
<div class="col-sm-12 col-md-6 col-lg-4">
  <% photo = place.photo.blank? ? 'awni2wkw3ak01469af6k' : place.photo %>
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path photo, crop: :fill %>')">
    <!-- <div class="card-icon"><i class="far fa-heart"></i></div> -->
  </div>
    <div class="place-name text-center">
      <h4><%= place.name %></h4>
      <p> <strong>€<%= place.price%></strong>/night</p>
  </div>
</div>
<% end %>
